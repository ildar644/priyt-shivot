# 🍎 Гайд для macOS: Запуск сайта приюта за 10 минут

## 🎯 Для кого этот гайд?
Для пользователей Mac, которые **НИКОГДА** не программировали и хотят просто запустить готовый сайт.

---

## ⚡ Супер-быстрый запуск

### 1. Откройте Terminal (Терминал):
- Нажмите `Cmd + Space`
- Введите "Terminal" и нажмите Enter
- Или найдите в Applications → Utilities → Terminal

### 2. Установите Homebrew (менеджер пакетов):
Вставьте эту команду в Terminal и нажмите Enter:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```
Дождитесь завершения (может занять 10-15 минут).

### 3. Установите Python:
```bash
brew install python
```

### 4. Скачайте проект:
```bash
cd ~/Desktop
curl -L https://github.com/ildar644/priyt-shivot/archive/refs/heads/main.zip -o priyt-shivot.zip
unzip priyt-shivot.zip
cd priyt-shivot-main/project
```

### 5. Создайте скрипт автоустановки:
```bash
cat > quick_start.sh << 'EOF'
#!/bin/bash
echo "🐾 Установка сайта приюта для животных..."
echo

echo "📦 Установка зависимостей..."
pip3 install -r requirements.txt

echo "⚙️ Настройка проекта..."
cp .env.example .env
python3 manage.py migrate
python3 manage.py collectstatic --noinput

echo "👤 Создание администратора..."
echo "Введите данные администратора:"
python3 manage.py createsuperuser

echo
echo "🎉 Установка завершена!"
echo "Сайт будет доступен по адресу: http://127.0.0.1:8000"
echo "Админ-панель: http://127.0.0.1:8000/admin/"
echo

echo "🚀 Запуск сервера..."
python3 manage.py runserver
EOF

chmod +x quick_start.sh
```

### 6. Запустите установку:
```bash
./quick_start.sh
```

### 7. Откройте сайт:
Откройте браузер и перейдите на: http://127.0.0.1:8000

---

## 🔧 Подробная инструкция

### Шаг 1: Проверка системы
Откройте Terminal и проверьте, установлен ли Python:
```bash
python3 --version
```

Если Python не установлен, переходите к следующему шагу.

### Шаг 2: Установка инструментов разработки
```bash
xcode-select --install
```
Нажмите "Install" в появившемся окне.

### Шаг 3: Установка Homebrew
Homebrew - это менеджер пакетов для Mac, который упрощает установку программ.

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

После установки добавьте Homebrew в PATH:
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

### Шаг 4: Установка Python
```bash
brew install python
```

Проверьте установку:
```bash
python3 --version
pip3 --version
```

### Шаг 5: Скачивание проекта

**Вариант A: Через Git (рекомендуется)**
```bash
brew install git
git clone https://github.com/ildar644/priyt-shivot.git
cd priyt-shivot/project
```

**Вариант B: Скачать ZIP**
```bash
cd ~/Desktop
curl -L https://github.com/ildar644/priyt-shivot/archive/refs/heads/main.zip -o priyt-shivot.zip
unzip priyt-shivot.zip
cd priyt-shivot-main/project
```

### Шаг 6: Установка зависимостей
```bash
pip3 install -r requirements.txt
```

Если возникают проблемы с правами доступа:
```bash
pip3 install --user -r requirements.txt
```

### Шаг 7: Настройка проекта
```bash
# Копирование файла настроек
cp .env.example .env

# Настройка базы данных
python3 manage.py migrate

# Сбор статических файлов
python3 manage.py collectstatic --noinput
```

### Шаг 8: Создание администратора
```bash
python3 manage.py createsuperuser
```

Введите:
- **Username**: admin (или любое имя)
- **Email**: admin@example.com (или ваш email)
- **Password**: создайте пароль
- **Password (again)**: повторите пароль

### Шаг 9: Запуск сервера
```bash
python3 manage.py runserver
```

### Шаг 10: Открытие сайта
Откройте браузер (Safari, Chrome, Firefox) и перейдите на:
http://127.0.0.1:8000

---

## 🎨 Что делать дальше?

### Добавление питомцев:
1. Откройте: http://127.0.0.1:8000/admin/
2. Войдите под созданным администратором
3. Добавьте приюты, типы животных, питомцев

### Остановка сервера:
В Terminal нажмите `Ctrl + C`

### Повторный запуск:
```bash
cd путь/к/проекту/project
python3 manage.py runserver
```

### Создание ярлыка для быстрого запуска:
```bash
cat > ~/Desktop/start_shelter.command << 'EOF'
#!/bin/bash
cd ~/Desktop/priyt-shivot-main/project
python3 manage.py runserver
EOF

chmod +x ~/Desktop/start_shelter.command
```

Теперь можно запускать сайт двойным кликом по файлу на рабочем столе.

---

## 🚨 Частые проблемы и решения

### "command not found: python3"
**Решение**: Установите Python через Homebrew
```bash
brew install python
```

### "command not found: pip3"
**Решение**: 
```bash
python3 -m ensurepip --upgrade
```

### "Permission denied"
**Решение**: Используйте флаг --user
```bash
pip3 install --user -r requirements.txt
```

### "Port already in use"
**Решение**: Используйте другой порт
```bash
python3 manage.py runserver 8080
```
Затем откройте: http://127.0.0.1:8080

### Проблемы с SSL сертификатами
**Решение**:
```bash
/Applications/Python\ 3.x/Install\ Certificates.command
```
(замените 3.x на вашу версию Python)

### "xcrun: error: invalid active developer path"
**Решение**: Установите Xcode Command Line Tools
```bash
xcode-select --install
```

---

## 🔧 Альтернативные способы установки Python

### Через официальный сайт:
1. Перейдите на https://www.python.org/downloads/
2. Скачайте установщик для macOS
3. Запустите .pkg файл
4. Следуйте инструкциям установщика

### Через pyenv (для продвинутых):
```bash
brew install pyenv
pyenv install 3.11.5
pyenv global 3.11.5
```

---

## 🌐 Доступ из локальной сети

Чтобы другие устройства в вашей сети могли открыть сайт:

1. Узнайте свой IP адрес:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

2. Запустите сервер:
```bash
python3 manage.py runserver 0.0.0.0:8000
```

3. Другие устройства смогут открыть сайт по адресу:
`http://ваш_ip:8000`

---

## 📁 Полезные команды Terminal

### Навигация:
```bash
pwd                    # Показать текущую папку
ls                     # Показать содержимое папки
ls -la                 # Показать все файлы с подробностями
cd папка               # Перейти в папку
cd ..                  # Перейти на уровень выше
cd ~                   # Перейти в домашнюю папку
```

### Работа с файлами:
```bash
cp файл новый_файл     # Копировать файл
mv файл новое_место    # Переместить файл
rm файл                # Удалить файл
mkdir папка            # Создать папку
```

### Просмотр содержимого файлов:
```bash
cat файл               # Показать содержимое файла
less файл              # Показать содержимое с прокруткой
head файл              # Показать первые строки
tail файл              # Показать последние строки
```

---

## 🔍 Диагностика проблем

### Проверка установки Python:
```bash
which python3
python3 --version
pip3 --version
```

### Проверка установленных пакетов:
```bash
pip3 list
```

### Проверка Django:
```bash
python3 -c "import django; print(django.get_version())"
```

### Просмотр логов сервера:
```bash
python3 manage.py runserver --verbosity=2
```

### Проверка настроек проекта:
```bash
python3 manage.py check
```

---

## 🎯 Автоматизация с помощью скриптов

### Создание полного скрипта установки:
```bash
cat > install_shelter.sh << 'EOF'
#!/bin/bash

echo "🐾 Автоматическая установка сайта приюта для животных"
echo "=================================================="

# Проверка Homebrew
if ! command -v brew &> /dev/null; then
    echo "📦 Установка Homebrew..."
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
fi

# Установка Python
echo "🐍 Установка Python..."
brew install python

# Скачивание проекта
echo "📥 Скачивание проекта..."
cd ~/Desktop
curl -L https://github.com/ildar644/priyt-shivot/archive/refs/heads/main.zip -o priyt-shivot.zip
unzip -q priyt-shivot.zip
cd priyt-shivot-main/project

# Установка зависимостей
echo "📦 Установка зависимостей..."
pip3 install -r requirements.txt

# Настройка проекта
echo "⚙️ Настройка проекта..."
cp .env.example .env
python3 manage.py migrate
python3 manage.py collectstatic --noinput

echo "✅ Установка завершена!"
echo "Для создания администратора выполните:"
echo "python3 manage.py createsuperuser"
echo
echo "Для запуска сервера выполните:"
echo "python3 manage.py runserver"
EOF

chmod +x install_shelter.sh
./install_shelter.sh
```

---

## 📱 Создание приложения (для продвинутых)

Можно создать настоящее Mac приложение:

```bash
# Установка py2app
pip3 install py2app

# Создание setup.py
cat > setup.py << 'EOF'
from setuptools import setup

APP = ['manage.py']
OPTIONS = {
    'argv_emulation': True,
    'packages': ['django'],
}

setup(
    app=APP,
    options={'py2app': OPTIONS},
    setup_requires=['py2app'],
)
EOF

# Создание приложения
python3 setup.py py2app
```

---

## ✅ Финальный чек-лист

- [ ] Terminal открыт и работает
- [ ] Homebrew установлен (`brew --version`)
- [ ] Python установлен (`python3 --version`)
- [ ] Проект скачан и распакован
- [ ] Зависимости установлены (`pip3 install -r requirements.txt`)
- [ ] База данных настроена (`python3 manage.py migrate`)
- [ ] Администратор создан (`python3 manage.py createsuperuser`)
- [ ] Сервер запущен (`python3 manage.py runserver`)
- [ ] Сайт открывается (http://127.0.0.1:8000)

**Если все галочки стоят - отлично! Сайт работает на вашем Mac! 🎉🐾**

---

## 🆘 Получение помощи

1. **Перечитайте инструкции** - большинство проблем решается повторным выполнением команд
2. **Проверьте раздел "Частые проблемы"**
3. **Создайте Issue**: https://github.com/ildar644/priyt-shivot/issues
4. **Приложите вывод команд** и опишите проблему

**Удачи в помощи животным! 🐾❤️**