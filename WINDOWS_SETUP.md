# 🪟 Гайд для Windows: Запуск сайта приюта за 10 минут

## 🎯 Для кого этот гайд?
Для пользователей Windows, которые **НИКОГДА** не программировали и хотят просто запустить готовый сайт.

---

## ⚡ Супер-быстрый запуск (для нетерпеливых)

### 1. Скачайте все необходимое:
- **Python**: https://www.python.org/downloads/ (нажмите большую желтую кнопку)
- **Проект**: https://github.com/ildar644/priyt-shivot (зеленая кнопка "Code" → "Download ZIP")

### 2. Установите Python:
- Запустите скачанный файл
- ✅ **ОБЯЗАТЕЛЬНО поставьте галочку "Add Python to PATH"**
- Нажмите "Install Now"
- Дождитесь завершения

### 3. Распакуйте проект:
- Распакуйте ZIP в папку `C:\priyt-shivot\`
- Откройте папку `C:\priyt-shivot\project\`

### 4. Создайте файл запуска:
- Кликните правой кнопкой в папке project
- "Создать" → "Текстовый документ"
- Назовите его `start.bat`
- Откройте и вставьте:

```batch
@echo off
title Приют для животных - Установка
color 0A
echo ================================
echo   ПРИЮТ ДЛЯ ЖИВОТНЫХ - УСТАНОВКА
echo ================================
echo.

echo [1/6] Обновление pip...
python -m pip install --upgrade pip --quiet

echo [2/6] Установка зависимостей...
pip install -r requirements.txt --quiet

echo [3/6] Создание настроек...
if not exist .env copy .env.example .env

echo [4/6] Настройка базы данных...
python manage.py migrate --verbosity=0

echo [5/6] Сбор статических файлов...
python manage.py collectstatic --noinput --verbosity=0

echo [6/6] Создание администратора...
echo.
echo Сейчас нужно создать администратора сайта:
python manage.py createsuperuser

echo.
echo ================================
echo   УСТАНОВКА ЗАВЕРШЕНА!
echo ================================
echo.
echo Сайт будет доступен по адресу:
echo http://127.0.0.1:8000
echo.
echo Админ-панель:
echo http://127.0.0.1:8000/admin/
echo.
echo Нажмите любую клавишу для запуска...
pause > nul

echo Запуск сервера...
python manage.py runserver
```

### 5. Запустите:
- Дважды кликните по `start.bat`
- Следуйте инструкциям на экране
- Откройте браузер: http://127.0.0.1:8000

---

## 🔧 Подробная инструкция (если что-то пошло не так)

### Шаг 1: Проверка системы
1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. В черном окне введите: `python --version`
4. Если появилась версия Python - отлично!
5. Если ошибка - переходите к установке Python

### Шаг 2: Установка Python (подробно)
1. Откройте https://www.python.org/downloads/
2. Нажмите желтую кнопку "Download Python X.X.X"
3. Дождитесь скачивания файла
4. Запустите скачанный файл (обычно в папке "Загрузки")
5. **ВАЖНО**: Поставьте галочку "Add Python to PATH" внизу окна
6. Нажмите "Install Now"
7. Дождитесь установки (может занять 5-10 минут)
8. Нажмите "Close"

### Шаг 3: Проверка установки Python
1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Введите: `python --version`
4. Должно появиться что-то вроде "Python 3.11.5"
5. Введите: `pip --version`
6. Должно появиться что-то вроде "pip 23.2.1"

Если команды не работают:
- Перезагрузите компьютер
- Попробуйте `py --version` вместо `python --version`

### Шаг 4: Скачивание проекта
1. Откройте https://github.com/ildar644/priyt-shivot
2. Нажмите зеленую кнопку "Code"
3. Выберите "Download ZIP"
4. Дождитесь скачивания
5. Откройте папку "Загрузки"
6. Кликните правой кнопкой по ZIP файлу
7. Выберите "Извлечь все..."
8. Выберите место (например, `C:\`)
9. Нажмите "Извлечь"

### Шаг 5: Переход в папку проекта
1. Откройте проводник Windows
2. Перейдите в папку, куда распаковали проект
3. Откройте папку `priyt-shivot-main` (или похожую)
4. Откройте папку `project`
5. Вы должны увидеть файлы: `manage.py`, `requirements.txt` и другие

### Шаг 6: Открытие командной строки в папке проекта
**Способ 1 (простой):**
1. В папке project нажмите Shift + правая кнопка мыши
2. Выберите "Открыть окно PowerShell здесь" или "Открыть командную строку здесь"

**Способ 2:**
1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Введите: `cd C:\путь\к\вашей\папке\project`

### Шаг 7: Установка зависимостей
В командной строке введите по очереди:
```batch
python -m pip install --upgrade pip
pip install -r requirements.txt
```

Дождитесь завершения (может занять 5-10 минут).

### Шаг 8: Настройка проекта
```batch
copy .env.example .env
python manage.py migrate
python manage.py collectstatic --noinput
```

### Шаг 9: Создание администратора
```batch
python manage.py createsuperuser
```

Введите:
- **Username**: admin (или любое имя)
- **Email**: admin@example.com (или ваш email)
- **Password**: создайте пароль (при вводе символы не отображаются - это нормально)
- **Password (again)**: повторите пароль

### Шаг 10: Запуск сайта
```batch
python manage.py runserver
```

Вы увидите что-то вроде:
```
Starting development server at http://127.0.0.1:8000/
```

### Шаг 11: Открытие сайта
1. Откройте любой браузер (Chrome, Firefox, Edge)
2. В адресной строке введите: `http://127.0.0.1:8000`
3. Нажмите Enter

**Поздравляем! Сайт работает! 🎉**

---

## 🎨 Что делать дальше?

### Добавление питомцев:
1. Откройте: http://127.0.0.1:8000/admin/
2. Войдите под созданным администратором
3. Добавьте приюты, типы животных, питомцев

### Остановка сервера:
- В командной строке нажмите `Ctrl + C`

### Повторный запуск:
1. Откройте командную строку в папке project
2. Введите: `python manage.py runserver`

---

## 🚨 Частые проблемы и решения

### "python не является внутренней командой"
**Причина**: Python не установлен или не добавлен в PATH
**Решение**: 
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или используйте `py` вместо `python`

### "pip не найден"
**Решение**:
```batch
python -m ensurepip --upgrade
```

### "Ошибка при установке пакетов"
**Решение**:
```batch
pip install --upgrade pip
pip install -r requirements.txt --user
```

### "Порт уже используется"
**Решение**: Используйте другой порт
```batch
python manage.py runserver 8080
```
Затем откройте: http://127.0.0.1:8080

### Сайт не открывается
**Проверьте**:
1. Сервер запущен (в командной строке должно быть написано "Starting development server")
2. Правильный адрес: http://127.0.0.1:8000 (не https!)
3. Попробуйте http://localhost:8000

### Забыли пароль администратора
**Решение**:
```batch
python manage.py createsuperuser
```
Создайте нового администратора.

---

## 📁 Структура файлов (для понимания)

```
priyt-shivot-main/
├── project/                 ← Основная папка проекта
│   ├── manage.py           ← Главный файл Django
│   ├── requirements.txt    ← Список зависимостей
│   ├── .env.example       ← Пример настроек
│   ├── .env               ← Ваши настройки (создается автоматически)
│   ├── db.sqlite3         ← База данных (создается автоматически)
│   ├── game/              ← Код приложения
│   ├── templates/         ← HTML шаблоны
│   ├── static/            ← CSS, JS, изображения
│   └── media/             ← Загруженные файлы
├── README.md              ← Описание проекта
└── SETUP_GUIDE.md         ← Этот гайд
```

---

## 🎯 Полезные команды

### Проверка статуса:
```batch
python manage.py check
```

### Просмотр всех команд:
```batch
python manage.py help
```

### Создание резервной копии данных:
```batch
python manage.py dumpdata > backup.json
```

### Восстановление данных:
```batch
python manage.py loaddata backup.json
```

---

## 🌐 Доступ из локальной сети

Чтобы другие устройства в вашей сети могли открыть сайт:

1. Узнайте свой IP адрес:
```batch
ipconfig
```
Найдите строку "IPv4-адрес" (например, 192.168.1.100)

2. Запустите сервер:
```batch
python manage.py runserver 0.0.0.0:8000
```

3. Другие устройства смогут открыть сайт по адресу:
`http://ваш_ip:8000` (например, http://192.168.1.100:8000)

---

## 🔒 Безопасность

### Для локального использования:
- Текущие настройки безопасны

### Для размещения в интернете:
- Измените `DEBUG = False` в настройках
- Установите сложный `SECRET_KEY`
- Настройте HTTPS
- Используйте PostgreSQL вместо SQLite

---

## 📞 Нужна помощь?

1. **Перечитайте гайд** - 90% проблем решаются повторным выполнением инструкций
2. **Проверьте раздел "Частые проблемы"**
3. **Создайте Issue на GitHub**: https://github.com/ildar644/priyt-shivot/issues
4. **Приложите скриншот ошибки** и опишите, что делали

---

## ✅ Финальный чек-лист

- [ ] Python установлен (`python --version` работает)
- [ ] Проект скачан и распакован
- [ ] Командная строка открыта в папке project
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] База данных настроена (`python manage.py migrate`)
- [ ] Администратор создан (`python manage.py createsuperuser`)
- [ ] Сервер запущен (`python manage.py runserver`)
- [ ] Сайт открывается (http://127.0.0.1:8000)

**Если все галочки стоят - вы молодец! Сайт работает! 🎉🐾**