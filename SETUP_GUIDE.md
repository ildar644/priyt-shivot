# 🚀 Полный гайд по развертыванию проекта "Приют для животных"

## 📋 Что это за проект?
Веб-сайт приюта для животных на Django с функциями:
- Просмотр питомцев
- Подача заявок на усыновление
- Система избранного
- Комментарии и отзывы
- Авторизация через Google
- Админ-панель для управления

---

## 🎯 Для кого этот гайд?
Для людей с **НУЛЕВЫМИ** знаниями программирования, которые хотят запустить проект на любом компьютере.

---

## 📚 Содержание
1. [Быстрый старт (5 минут)](#быстрый-старт)
2. [Подробная установка](#подробная-установка)
3. [Настройка Google OAuth](#настройка-google-oauth)
4. [Решение проблем](#решение-проблем)
5. [Дополнительные возможности](#дополнительные-возможности)

---

## ⚡ Быстрый старт

### Шаг 1: Скачайте проект
1. Откройте браузер
2. Перейдите на: https://github.com/ildar644/priyt-shivot
3. Нажмите зеленую кнопку **"Code"**
4. Выберите **"Download ZIP"**
5. Распакуйте архив в любую папку

### Шаг 2: Установите Python
1. Перейдите на: https://www.python.org/downloads/
2. Скачайте последнюю версию Python
3. **ВАЖНО**: При установке поставьте галочку **"Add Python to PATH"**
4. Нажмите **"Install Now"**

### Шаг 3: Запустите проект
1. Откройте папку с проектом
2. Найдите файл `QUICK_START.bat` (если его нет, создайте - см. ниже)
3. Дважды кликните по нему
4. Дождитесь завершения установки
5. Откройте браузер и перейдите на: http://127.0.0.1:8000

### Создание QUICK_START.bat (если его нет):
Создайте текстовый файл и вставьте:
```batch
@echo off
echo Установка проекта "Приют для животных"...
cd /d "%~dp0project"
python -m pip install --upgrade pip
pip install -r requirements.txt
copy .env.example .env
python manage.py migrate
python manage.py collectstatic --noinput
echo.
echo Создание администратора...
python manage.py createsuperuser
echo.
echo Запуск сервера...
python manage.py runserver
pause
```
Сохраните как `QUICK_START.bat`

---

## 🔧 Подробная установка

### Системные требования
- **Операционная система**: Windows 10/11, macOS, Linux
- **Свободное место**: минимум 500 МБ
- **Интернет**: для скачивания зависимостей

### Шаг 1: Установка Python

#### Windows:
1. Скачайте Python с https://www.python.org/downloads/
2. Запустите установщик
3. ✅ **Обязательно поставьте галочку "Add Python to PATH"**
4. Выберите "Install Now"
5. Дождитесь завершения

#### macOS:
```bash
# Установите Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите Python
brew install python
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

### Шаг 2: Проверка установки
Откройте командную строку (Windows: Win+R → cmd) и выполните:
```bash
python --version
pip --version
```
Должны появиться версии Python и pip.

### Шаг 3: Скачивание проекта

#### Вариант A: Через Git (рекомендуется)
```bash
git clone https://github.com/ildar644/priyt-shivot.git
cd priyt-shivot
```

#### Вариант B: Скачать ZIP
1. Перейдите на https://github.com/ildar644/priyt-shivot
2. Нажмите "Code" → "Download ZIP"
3. Распакуйте в любую папку

### Шаг 4: Установка зависимостей
```bash
cd project
pip install -r requirements.txt
```

### Шаг 5: Настройка окружения
```bash
# Скопируйте файл настроек
copy .env.example .env    # Windows
cp .env.example .env      # macOS/Linux
```

### Шаг 6: Настройка базы данных
```bash
python manage.py migrate
```

### Шаг 7: Создание администратора
```bash
python manage.py createsuperuser
```
Введите:
- Имя пользователя (например: admin)
- Email (можно любой)
- Пароль (запомните его!)

### Шаг 8: Сбор статических файлов
```bash
python manage.py collectstatic --noinput
```

### Шаг 9: Запуск сервера
```bash
python manage.py runserver
```

### Шаг 10: Открытие сайта
Откройте браузер и перейдите на: http://127.0.0.1:8000

---

## 🔑 Настройка Google OAuth

### Зачем это нужно?
Позволяет пользователям входить через Google аккаунт.

### Пошаговая настройка:

#### 1. Создание проекта в Google Console
1. Перейдите на: https://console.developers.google.com/
2. Войдите в Google аккаунт
3. Нажмите **"Создать проект"**
4. Введите название: "Приют для животных"
5. Нажмите **"Создать"**

#### 2. Включение Google+ API
1. В левом меню выберите **"API и сервисы"** → **"Библиотека"**
2. Найдите **"Google+ API"**
3. Нажмите **"Включить"**

#### 3. Создание учетных данных
1. Перейдите в **"API и сервисы"** → **"Учетные данные"**
2. Нажмите **"+ Создать учетные данные"**
3. Выберите **"Идентификатор клиента OAuth"**
4. Выберите тип приложения: **"Веб-приложение"**
5. Введите название: "Приют OAuth"
6. В **"Разрешенные URI перенаправления"** добавьте:
   - `http://127.0.0.1:8000/accounts/google/login/callback/`
   - `http://localhost:8000/accounts/google/login/callback/`
7. Нажмите **"Создать"**

#### 4. Получение ключей
1. Скопируйте **"Идентификатор клиента"**
2. Скопируйте **"Секрет клиента"**

#### 5. Настройка проекта
1. Откройте файл `.env` в папке project
2. Замените строки:
```
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
```
На ваши реальные ключи:
```
GOOGLE_OAUTH_CLIENT_ID=ваш_идентификатор_клиента
GOOGLE_OAUTH_CLIENT_SECRET=ваш_секрет_клиента
```

#### 6. Применение настроек
```bash
python manage.py migrate
python manage.py runserver
```

---

## 🚨 Решение проблем

### Проблема: "python не является внутренней командой"
**Решение**: Python не добавлен в PATH
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или используйте `py` вместо `python`

### Проблема: "pip не найден"
**Решение**:
```bash
python -m ensurepip --upgrade
```

### Проблема: "Ошибка при установке зависимостей"
**Решение**:
```bash
pip install --upgrade pip
pip install -r requirements.txt --user
```

### Проблема: "Порт 8000 занят"
**Решение**: Используйте другой порт
```bash
python manage.py runserver 8080
```
Затем откройте: http://127.0.0.1:8080

### Проблема: "Ошибка базы данных"
**Решение**:
```bash
python manage.py migrate --run-syncdb
```

### Проблема: "Статические файлы не загружаются"
**Решение**:
```bash
python manage.py collectstatic --clear --noinput
```

### Проблема: "Google OAuth не работает"
**Решение**:
1. Проверьте правильность ключей в `.env`
2. Убедитесь, что URI перенаправления правильные
3. Проверьте, что Google+ API включен

---

## 🎨 Дополнительные возможности

### Добавление питомцев
1. Перейдите на: http://127.0.0.1:8000/admin/
2. Войдите под администратором
3. Добавьте приюты, типы питомцев, самих питомцев

### Настройка email
В файле `.env` добавьте:
```
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=ваш_email@gmail.com
EMAIL_HOST_PASSWORD=ваш_пароль_приложения
EMAIL_USE_TLS=True
```

### Настройка PostgreSQL (продвинутый уровень)
1. Установите PostgreSQL
2. Создайте базу данных
3. Обновите настройки в `.env`:
```
DB_NAME=pets_shelter_db
DB_USER=postgres
DB_PASSWORD=ваш_пароль
DB_HOST=localhost
DB_PORT=5432
```

### Деплой на сервер
Для размещения в интернете используйте:
- **Heroku** (бесплатно для начала)
- **PythonAnywhere** (простой в настройке)
- **DigitalOcean** (для продвинутых)

---

## 📞 Получение помощи

### Если что-то не работает:
1. Проверьте [раздел решения проблем](#решение-проблем)
2. Посмотрите логи в командной строке
3. Создайте Issue на GitHub: https://github.com/ildar644/priyt-shivot/issues

### Полезные команды для диагностики:
```bash
# Проверка версий
python --version
pip --version

# Проверка установленных пакетов
pip list

# Проверка настроек Django
python manage.py check

# Просмотр логов
python manage.py runserver --verbosity=2
```

---

## ✅ Чек-лист успешной установки

- [ ] Python установлен и работает
- [ ] Проект скачан и распакован
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и настроен
- [ ] База данных мигрирована (`python manage.py migrate`)
- [ ] Суперпользователь создан (`python manage.py createsuperuser`)
- [ ] Статические файлы собраны (`python manage.py collectstatic`)
- [ ] Сервер запускается (`python manage.py runserver`)
- [ ] Сайт открывается в браузере (http://127.0.0.1:8000)
- [ ] Админ-панель доступна (http://127.0.0.1:8000/admin/)

---

## 🎉 Поздравляем!

Если все пункты выполнены, ваш сайт приюта для животных готов к работе!

**Что дальше?**
1. Добавьте питомцев через админ-панель
2. Настройте Google OAuth для удобного входа
3. Пригласите друзей протестировать сайт
4. При необходимости разместите в интернете

**Удачи в помощи животным! 🐾❤️**